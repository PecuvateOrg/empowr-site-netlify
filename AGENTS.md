# Empowr Main Site Agent Instructions

Read `CLAUDE.md` first. It is the canonical MWP Layer 0 file for this project and contains routing, project-wide rules, deployment metadata, and token-management guidance.

Then read `CONTEXT.md` when relevant to the task.

This repo is **public** (`PecuvateOrg/empowr-site-netlify`). Its `memory.md` and `DEVLOG.md`
are not kept here — they live in the private Empowr CIC hub at
`../workspace-docs/empowr-site-netlify/`. Read and write them there. Never recreate either
file in this repo; both are gitignored and would be silently lost.

Agent role definitions live in `agent-roles.md`.
